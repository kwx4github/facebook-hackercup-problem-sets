#include <stdio.h>

int main()
{
	#ifndef ONLINE_JUDGE
   freopen("input.txt", "rt", stdin);
   freopen("output.txt", "wt", stdout);
	#endif
	int T,m;
	scanf("%d",&T);
	for(m=1;m<=T;m++)
	{
		int N;
		scanf("%d",&N);
		printf("Case #%d: %d %d\n",m,N,N);
	}
}

#include<stdio.h>
#include<conio.h>
int main()
{
int r,c,m,t,p=1,**a;
printf("no of test cases");
scanf("%d",&t);
do
{
printf("enter no. of rows and columns can't>5",);
scanf("%d%d",&r,&c);
a=(int**)calloc(r,sizeof(int));
for(i=0;i<r;i++)
{
  a[i]=int(*)calloc(c,sizeof(int));
}
printf("no of mines can't>%d",r*c);
scanf("%d",&m);

p=p+1;
}while(p<=t);
for(i=0;i<r;i++)
 { free(a[i]); }
 free(a);
 getch();
 return 0;
}

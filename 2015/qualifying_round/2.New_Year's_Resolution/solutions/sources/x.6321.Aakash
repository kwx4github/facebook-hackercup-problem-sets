#include<bits/stdc++.h>
using namespace std;
int main(){
	int t;
	cin>>t;
	while(t--){
		long long int b[10000],n,a,sum=0,i;
		cin>>n;
		for(i=0;i<n;i++){
			cin>>a;
			sum = sum + a;
			
		}
		if(sum>=100 && (sum-100)<n) cout<<"YES"<<endl;
		//else if(sum>100 && (sum-100) <= n-1) cout<<"YES";
			else cout <<"NO"<<endl ;
	}
	
}

